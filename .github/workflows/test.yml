on: [workflow_call]
defaults:
  run:
    shell: bash

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Go and dependencies
        uses: ./.github/reusable_workflows/setup-go-and-deps.yml
      - name: test
        run: go test -v ./...